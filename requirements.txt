# Runtime dependencies for the Vercel serverless deployment (the function is
# built from repo root). Dev/server extras live in backend/requirements.txt.
fastapi>=0.110
pydantic>=2.6
PyYAML>=6.0
fpdf2>=2.7
PyJWT>=2.8
SQLAlchemy>=2.0
psycopg[binary]>=3.1
anthropic>=0.40
python-multipart>=0.0.9
httpx>=0.27
